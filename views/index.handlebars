<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>

        .card{
            width: 20%;
            height: 400px;
            border: solid 1px;
            border-radius: 5px;
            color: #34403A;
            border-color:#EA2793;
            margin-bottom: 1rem;
            margin-left: 1rem;
            display: inline-block;
    
        }

        .button{
            width: 150px;
            height: 50px;
            border-radius: 8px;
            display:block;
            margin-bottom:1rem;
        }

        .primary-button{
            background-color: #EA2793;
        }

        .secundary-button{
            background-color: #fff;
            border:solid 3px;
            border-color: #EA2793;
        }
    </style>
</head>
<body>
    {{#each productosJson}}
    <div class="card"> 
        <h3 name = "nombre">Nombre: {{this.nombre}}</h3>
        <p>precio: {{this.precio}}</p>
        <p>descripcion: {{this.descripcion}}</p>
        <p>Imagen: {{this.thumbnail}}</p>
        <p>sku: {{this.sku}}</p>
        <p>stock: {{this.stock}}</p>
        <p>id: {{this.prodcutoId}}</p>

        <form action="/api/products/addToCart" method="post">            
        <button class="button primary-button" type="submit" name = "added"value = "{{this.prodcutoId}}" type="hidden">Añadir al carrito</button>

        <button class="button secundary-button" type="submit" name = "showCart"value = "true" type="hidden"><a href="http://localhost:8080/api/products/addToCart/view">ver productos</a></button>

        
        </form>
        
    </div>
    {{/each}}
</body>
</html>